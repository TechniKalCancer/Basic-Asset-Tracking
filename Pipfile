[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=3.0"
flask-sqlalchemy = ">=3.1"
python-dotenv = ">=1.0"
werkzeug = ">=3.0"

[dev-packages]

[requires]
python_version = "3.12"
